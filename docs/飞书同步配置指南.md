# 飞书多维表格同步配置指南（小白版）

> 适用版本：v3.5（桌面便携版 2.5.0）及以后（2026-09-12 更新：配置页新增「选择数据表」下拉）
> 阅读对象：需要把固定资产系统与飞书多维表格互相同步的使用者，**无需任何编程基础**。
> 配置一次约 3~5 分钟，之后点按钮即可双向同步。

---

## 一、功能说明

系统可以和**飞书多维表格（Base/Bitable）**双向同步资产数据：

| 方向 | 含义 | 典型场景 |
|---|---|---|
| **推送到飞书**（系统 → 飞书） | 把系统里的资产写入飞书表，已存在的更新、不存在的新增 | 资产盘点后上报、领导要飞书报表 |
| **从飞书拉取**（飞书 → 系统） | 把飞书表里的记录读入系统，自动新增/更新 | 多人在飞书里登记资产后汇总进系统 |

- 同步是**手动触发**的（点按钮才同步），不会自动后台运行
- 同步以"资产编号"作为匹配主键：编号相同 = 同一条资产
- 支持**多数据表**：一个多维表格下可以有多个公司/主体的数据表（如"花满堂""有花说"）。配置页提供「**选择数据表**」下拉，可直接列出该多维表格下全部数据表供选择；**Table ID 留空**时推送按资产的"主体"字段自动路由到对应表，拉取会读取全部数据表
- 冲突处理：默认"最后写入获胜（LWW）"，即谁最后修改以谁为准

---

## 二、前置条件

| 条件 | 说明 |
|---|---|
| 系统运行在 **C/S 模式** 或 **新版单机模式** | 飞书卡片仅在这两种模式显示：① Electron 客户端连接了服务器；② 浏览器访问服务器；③ v2.5.0+ 便携版单机模式（已内置完整服务端）。**旧的纯单机精简模式无飞书功能** |
| 飞书账号有创建应用权限 | 需要能登录 [open.feishu.cn](https://open.feishu.cn/app) 创建"企业自建应用"（个人飞书若无入口，找企业管理员开通或由管理员代建） |
| 服务器能访问飞书 | 服务器（或单机电脑）需能访问 `open.feishu.cn`（公网）。纯内网隔离环境不可用 |
| 管理员账号配置 | 飞书配置仅 **admin（管理员）** 可保存；普通用户可看到卡片但不能改配置 |

---

## 三、配置步骤（共 3 步）

### 第 1 步：创建飞书应用，拿到 App ID 和 App Secret

1. 浏览器打开 **<https://open.feishu.cn/app>**，用飞书账号登录
2. 点 **「创建企业自建应用」**，名称随便填（如"固定资产同步"），创建
3. 左侧菜单进入 **「权限管理」**，搜索 **多维表格**，勾选开通以下权限（找不到全部时，勾选名称含"多维表格"的读写权限即可）：
   - `bitable:app` —— 查看、评论、编辑和管理多维表格（最省事，勾这一个即可）
   - 或分别勾选：查看多维表格 / 编辑多维表格 等细项
4. 左侧菜单进入 **「版本管理与发布」** → 创建版本 → **申请发布**（企业管理员在飞书管理后台审批通过后生效；自建应用通常本人即可审批）
5. 回到左侧 **「凭证与基础信息」** 页，能看到：
   - **App ID**：形如 `cli_aa13xxxxxxxxx`
   - **App Secret**：点"显示"后复制（一串字母数字，**相当于密码，不要发到群里**）

### 第 2 步：准备多维表格，并把应用加进去

1. 在飞书里打开（或新建）一个**多维表格**（注意：是"多维表格/Base"，不是普通文档/电子表格）
2. 点右上角 **「···」→「更多」→「添加文档应用」**（或"···"→"添加应用"），搜索第 1 步创建的应用名（如"固定资产同步"），**添加**
   - ⚠️ 这一步不做，系统访问时会报无权限/找不到表
3. 点右上角 **「分享」→「复制链接」**，得到形如：
   ```
   https://xxx.feishu.cn/base/PWi1bPUb4ayCfrsyYj4caDM1nfg?table=tblXxxxxxxxxxx&view=...
   ```
   - `/base/` 后面那串 = **App Token**（多维表格标识）
   - `?table=` 后面那串 = **Table ID**（具体数据表标识）
   - ⚠️ 注意：在飞书里**删除数据表后重建，Table ID 会变**，旧链接里的 ID 随即失效。这就是为什么推荐用配置页的「选择数据表」下拉而不是手抄 ID（见第 3 步）

> 表结构要求：表里要有一列**资产编号**（文本类型），作为同步主键；其余字段可自由命名，系统会自动按名称匹配（见第 3 步）。

### 第 3 步：在系统里填入配置（3 分钟）

1. 登录固定资产系统 → 左侧 **「系统设置」** → 找到 **「飞书多维表格同步」** 卡片
   - 看不到卡片？见 [FAQ](#五常见问题)
2. **（最省事）** 在卡片顶部绿色框 **「✨ 懒人方式」** 里粘贴第 2 步复制的飞书链接 → 点 **「解析填入」**
   - 系统自动填好 **App Token** 和 **Table ID**，显示绿色"✓ 已提取"提示
   - 同时自动刷新「选择数据表」下拉并选中链接中的表，随后自动加载该表字段
   - 想同步该多维表格下**所有数据表**：在下拉里选「— 全部数据表(按主体自动路由) —」（即把 Table ID 清空）
3. 手动填入第 1 步拿到的 **App ID** 和 **App Secret**
   - App Secret 框默认遮罩显示，勾选"显示"可核对
   - 每个输入框旁的 **❓** 鼠标悬停可看"这是什么、去哪找"
4. 点 **「测试连接」**：
   - ✅ 成功 → 绿色提示"连接成功，字段数 N"
   - ❌ 失败 → 按提示排查（常见原因见 FAQ），**不用保存也能测试**
5. **选择数据表**（Table ID 输入框下方的下拉）：
   - 填好 App Token 后下拉会**自动列出**该多维表格下的全部数据表（表名 + Table ID），也可点右侧 **「刷新」** 按钮重新获取
   - 下拉中选中一张表 → 系统**自动静默保存并立即重新加载该表字段**，无需再点"加载字段"
   - 字段映射以**所选表的结构为参考**；多主体场景也可以选「— 全部数据表 —」，此时拉取遍历所有表、推送按"主体"自动路由
   - 若已保存的数据表被删除/重建，下拉会出现红色警告 `⚠ tblxxx 已不存在(可能被删除或重建), 请重新选择`，在下拉中重新选中新表即可
6. 点 **「加载字段」**（若第 5 步选表时已自动加载可跳过）：
   - 系统读取**所选数据表**的全部字段，并**自动生成字段映射表**（本地 18 个字段 ↔ 飞书字段，同名/近义词自动预选）
   - 下拉框里"— 不映射 —"表示该字段不同步，可手动调整每一行
   - 确认 **「资产编号对应字段」** 选的是飞书表里的资产编号列（默认自动选"资产编号"）
   - 注意：Table ID 为空（选了"全部数据表"）时不能加载字段——映射只需要任选一张结构有代表性的表作为参考即可
7. 点 **「保存配置」** → 完成！

> 配置保存在**服务器端**数据库，所有客户端共用一份，只需配置一次。

---

## 四、日常同步操作

配置完成后，在同一卡片底部：

| 按钮 | 作用 | 结果提示示例 |
|---|---|---|
| **推送到飞书** | 系统资产 → 飞书表（增量：只新增/更新有变化的） | `推送完成: 新增 5, 更新 2, 跳过 70` |
| **从飞书拉取** | 飞书表 → 系统（自动刷新资产列表） | `拉取完成: 新增 78, 更新 0, 跳过 0` |

- "跳过"= 两边内容一致，无需处理
- 推送/拉取前建议先确认数据方向；拉取入库前系统会逐条校验，缺资产编号等无效记录会计入"无效 N"并跳过，不影响其他记录
- 最近一次同步的时间、方向、数量显示在按钮下方状态栏；管理员也可在 **「用户管理 → 操作日志」** 看到 `feishu.sync.push/pull`、`feishu.config.save` 审计记录

### 同步选项说明

| 选项 | 含义 |
|---|---|
| 允许推送 / 允许拉取 | 勾选后对应按钮才可用（默认都勾） |
| 冲突策略：最后写入获胜（LWW，默认） | 同一条资产两边都改过，按修改时间新的覆盖旧的 |
| 冲突策略：本地优先 | 拉取时只新增飞书有而系统没有的，不覆盖已有记录 |
| 冲突策略：远端优先 | 拉取时无条件以飞书为准覆盖 |

---

## 五、常见问题（FAQ）

**Q：系统设置里看不到"飞书多维表格同步"卡片？**
A：三种可能：
1. 用的是旧版纯单机模式 —— 升级到 v2.5.0+ 便携版（单机模式已内置完整服务端，卡片自动出现），或改用 C/S 模式连接服务器
2. 浏览器/Electron 缓存了旧页面 —— 按 **Ctrl+Shift+R** 强制刷新
3. 服务器前端文件过旧 —— 让管理员更新服务器上的前端文件（见部署文档"前端热更新"）

**Q：测试连接报"飞书鉴权失败 / 99991663 等错误码"？**
A：App ID 或 App Secret 填错/复制时多了空格；或应用未发布生效。回 open.feishu.cn 核对凭证、确认版本已发布。

**Q：测试连接报"无权限 / 表格不存在 / 1254005"？**
A：应用没被添加到这个多维表格。回飞书表 →「···」→「添加文档应用」把应用加进去（第 2 步第 2 小步）。

**Q：测试连接报"权限不足 / 1254030"？**
A：应用缺多维表格权限。回开放平台「权限管理」勾全多维表格权限 → 重新发布版本 → 等 1~2 分钟再试。

**Q：连接失败提示 `1254041 TableIdNotFound`（字段全显示"不映射"）？**
A：配置里的 Table ID 在该多维表格中已不存在——通常是数据表被删除后重建（重建会生成**新 Table ID**），或该表被移动到了别的多维表格。处理：看配置页「选择数据表」下拉，失效的表会有红色 `⚠ 已不存在` 提示，点开下拉重新选中现在的数据表即可（选中后自动保存并重新加载字段）。注意：此错误只影响"字段加载/测试连接/推送兜底"，**拉取**遍历全部数据表不受影响。

**Q：「选择数据表」下拉一直显示"加载中"或"获取失败"？**
A：依次检查：① App ID / App Secret / App Token 是否已填且正确；② 应用是否已添加到该多维表格（第 2 步第 2 小步）；③ 服务器能否访问公网 `open.feishu.cn`。点右侧「刷新」重试；App Token 修改后下拉也会自动重新拉取。

**Q：解析链接提示"未能识别 App Token"？**
A：确认复制的是**多维表格**链接（URL 含 `/base/`）。知识库（wiki）内嵌的多维表格请先在多维表格里点"在新标签页打开"再复制链接；国际版域名 `larksuite.com` 同样支持解析。

**Q：字段映射表里某行下拉是空的（"— 不映射 —"）？**
A：飞书表里没有与该本地字段同名/近义的列。两种处理：① 在飞书表里新建对应列后重新"加载字段"；② 保持"不映射"，该字段不参与同步，不影响其他字段。

**Q：多主体（多家公司/门店）怎么用？**
A：在同一个多维表格里为每个主体建一张数据表（表名=主体名，如"花满堂"），下拉选「— 全部数据表(按主体自动路由) —」（Table ID 留空）。推送时系统按资产的"主体"字段值自动找同名表写入（首次需要先"从飞书拉取"一次以学习主体→数据表的对应关系；找不到会跳过并计入失败明细）；拉取时自动读取全部数据表。若只需操作其中一张表，也可以在下拉里单独选中它。

**Q：同步会删掉数据吗？**
A：不会。推送和拉取都只做"新增 + 更新"，**不会删除**任何一边的记录。在飞书里删掉的行，下次推送可能被重新创建（系统侧仍存在时）。

**Q：App Secret 安全吗？**
A：Secret 加密存储在服务器数据库，界面读取时显示为掩码（如 `cli****ab`）；配置接口仅管理员可访问。不要把 Secret 截图发给无关人员。

---

## 六、技术细节（管理员/运维参考）

| 项 | 说明 |
|---|---|
| 配置存储 | 服务器 SQLite `kv_store` 表，键 `feishu_sync_config`（JSON，含凭证、字段映射、同步选项、最近同步状态） |
| 多表路由存储 | `kv_store` 键 `feishu_owner_table_map`（主体 → table_id 自动学习映射） |
| 同步状态表 | `feishu_sync_state`（资产 ↔ 飞书 record_id 对应关系、字段哈希增量检测） |
| 调用接口 | 飞书开放平台 Bitable v1：tenant_access_token、tables、fields、records（list / batch_create / batch_update） |
| 限流处理 | 401 自动刷新 token 重试；429 指数退避重试 3 次；5xx 重试 2 次；批量接口每批 500 条串行 |
| 服务端 API | `GET/POST /api/feishu/config`、`POST /api/feishu/test-connection`、`GET/POST /api/feishu/tables`（列出多维表格下全部数据表，POST 支持表单临时凭证）、`GET/POST /api/feishu/fields`（POST 支持指定 tableId 临时加载）、`POST /api/feishu/fields/auto-detect`、`POST /api/feishu/sync/push`、`POST /api/feishu/sync/pull`、`GET /api/feishu/sync/status`（均需管理员鉴权） |
| 前端交互 | `js/feishu-sync.js`：进入页面自动拉数据表列表并选中已保存表；下拉切换→静默保存→自动重载字段；失效表红色警告；配置 DOM 在 `index.html`「飞书多维表格同步」卡片 |
| 单机内嵌模式 | v2.5.0+ 便携版单机启动时自动拉起内置 asset-server（127.0.0.1 随机端口 + 一次性 token 免密），飞书功能与 C/S 完全一致；数据在本机 SQLite，首次启动自动迁移旧版 JSON 数据 |
| 设计文档 | 开发细节见 `.trae/documents/feishu-bidirectional-sync.md` |
