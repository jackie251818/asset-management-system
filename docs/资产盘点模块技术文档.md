# 资产盘点模块 — 技术实现文档

> 本文档供开发人员或 AI 助手接手实现。包含完整的需求、架构、数据结构、API、前端模块、UI 布局、样式、移动端适配、验证方案。
>
> 项目路径：`d:\Users\Administrator\Desktop\固定资产管理系统exe离线便携版v1.1`
>
> 服务器：Ubuntu 22.04 @ 192.168.40.247，Nginx 反代 80 → 127.0.0.1:3456，SSH hmt（密码由内部运维保管）

---

## 实施状态：✅ 已完成并上线（2026-09-09）

> ⚠️ **移动端章节已过时（2026-09-12 更新）**：本文档下文"移动端适配/验证"章节描述的是旧 **Capacitor WebView 套壳**方案（`mobile-app/`、BarcodeScanner、`js/mobile-bridge.js` 循环扫码），该工程**已删除并被 React Native 原生 App 取代**。盘点的服务端 API、Web 端批次/快照/乐观锁逻辑仍然有效；现行移动端实现（VisionCamera v4 + MLKit、"一码一确认"弹窗交互、独立 RN 页面）以 **[手机App构建说明.md](手机App构建说明.md)** 和 `mobile-app-rn/src/screens/`（InventoryListScreen / InventoryDetailScreen / ScanScreen）为准。

本模块已于 2026-09-09 按本文档实施完成，7 个文件改动全部通过 `node --check` 语法校验与 HTML ID 交叉核对，并热部署到生产服务器 `192.168.40.247`（服务端 compat.js 重启 asset-server 生效，前端文件免重启）。Android APK 同步重建（6.24 MB）。

### 实施时与本文档原稿的差异（已落地）

以下为实施过程中对原稿的改进/修正，**以实际代码为准**：

1. **桌面端扫码降级不用原生 `prompt()`** — 原稿第 348 行用 `prompt('请输入资产编号...')`，但 Electron v30+ 会静默拦截原生 `prompt()`（项目既有教训）。改为新增自定义弹窗 `inv-manual-modal`（[index.html](file:///d:/Users/Administrator/Desktop/固定资产管理系统exe离线便携版v1.1/index.html)），支持空格/逗号/换行分隔**批量录入**，Enter 提交连续录入不关弹窗。
2. **删除批次用 `storageManager.removeItem`** — 原稿注释说"可能没有 removeItem，用 setItem 设为 null"。实测 `storageManager.removeItem` 存在（storage.js:1252，C/S 走 `DELETE /api/delete`），改用它真正删除 kv_store 记录，避免残留 null 值。
3. **`/api/delete` 路由同步放行 viewer** — 原稿只改了 `/api/save`，但删除批次走 `removeItem` → `DELETE /api/delete`，同样需要对 inventory 键放行 viewer（否则 viewer 删批次会 403）。已在 compat.js `/delete` 路由加 `isInventoryKey` 放行 + 审计。
4. **HTML 笔误修正** — 原稿第 707 行 `id="inv-counted""` 有重复双引号，已改为 `id="inv-counted"`。
5. **缓存版本号 bump** — `styles.css?v=3.4.4 → v=3.4.5`、`mobile-bridge.js?v=1.0.0 → v=1.1.0`（防止浏览器沿用旧缓存）。
6. **Excel 报告增补汇总 sheet** — 导出的 xlsx 除"盘点明细"外追加"汇总"工作表，列出应盘/已盘/未盘到/异常数量。
7. **删除当前批次后自动回列表** — `deleteSession` 删除当前打开的批次时调用 `backToList()`，避免界面停留在已失效的详情页。
8. **乐观锁冲突反馈用 toast** — `markItem` 冲突时用 `showNotification(msg, 'error')` 替代 `alert`，与项目其他模块通知方式一致。

### 部署记录

- 热上传：7 文件 pscp 到 `/tmp` → sudo cp 到 `/opt/asset-server/` → chown asset:asset → compat.js 重启服务
- 旧文件备份：服务器 `/tmp/inv-backup-20260909121645/`
- MD5 校验：7 文件本地与远程逐一匹配
- HTTP 验证：`/api/ping` 200、`/api/info` cs:true、inventory.js/index.html/styles.css/mobile-bridge.js 均含新内容
- APK 重建：`cap sync` + `gradlew clean assembleDebug` → app-debug.apk 6.24 MB（APK 仅含引导页，盘点功能从服务器加载）

---

## 一、需求概述

### 功能要求
1. **两种盘点方式**：扫码盘点（手机扫标签二维码自动标记）+ 列表勾选盘点（逐条手动标记）
2. **盘点状态**：每条资产记录为 已盘(counted) / 未盘到(pending) / 异常(exception)
3. **异常记录**：异常状态可填写备注（如"屏幕有划痕"、"位置不符"）
4. **盘点报告**：完成后生成统计报告（已盘/未盘到/异常数量 + 盘点率），可导出 Excel
5. **多轮历史**：每次盘点保存为独立批次，可查看历史盘点记录和详情
6. **权限**：所有登录用户（含 viewer 只读角色）均可发起和执行盘点

### 兼容性要求
- C/S 客户端模式（连远程服务器） ✓
- 单机内嵌模式（Electron EXE） ✓
- Capacitor APK 移动端 ✓
- 4 套主题（浅色/深色/纯黑/科技）自动适配 ✓

---

## 二、架构决策

### 存储方案：kv_store 扩展（非独立表）

盘点数据存入现有 `kv_store` 表（SQLite key-value 存储），通过 `/api/save` + `/api/load` 兼容层读写。

**选择理由：**
1. 复用 `storageManager.getItem/setItem` 三模式自动适配（本地 IndexedDB / Electron 内嵌 / C-S 远程），零基础设施改动
2. 不需要新建服务端路由文件、处理 CORS / JWT / 错误格式等重复工作
3. 与飞书同步配置、系统设置等现有 kv_store 数据使用同一套持久化通道

**乐观锁**：用 `version` 字段防止多用户并发覆盖，与现有 `assets.js` 乐观锁模式一致。

---

## 三、数据结构

### 3.1 批次列表

存储 key：`inventory_sessions`（kv_store 表）

```js
[
  {
    id: "inv_20260908_153000",          // 批次 ID（时间戳格式）
    name: "2026年9月例行盘点",          // 用户输入的批次名
    creator: "admin",                   // 创建人用户名
    creatorRole: "admin",               // 创建人角色
    createdAt: "2026-09-08T15:30:00",   // ISO 时间
    updatedAt: "2026-09-08T16:12:33",
    status: "in_progress",              // 'in_progress' | 'completed'
    scope: {                            // 资产范围筛选条件
      mode: "all" | "department" | "location",
      department: "技术部",            // mode=department 时
      location: "北京机房"              // mode=location 时
    },
    totalAssets: 156,                   // 应盘数量
    countedAssets: 89,                  // 已盘数量（缓存值，以明细为准）
    exceptionCount: 3,                 // 异常数量（缓存值）
    version: 5                          // 乐观锁版本号
  }
]
```

### 3.2 单批次明细

存储 key：`inventory_session_<id>`（如 `inventory_session_inv_20260908_153000`）

```js
{
  id: "inv_20260908_153000",
  session: { /* 与列表项一致的全部字段，完整副本 */ },
  version: 5,                           // 与列表项 version 同步
  items: [
    {
      assetId: "FYM-001",               // 资产编号
      snapshot: {                       // 资产快照（创建时冗余，防后续删/改）
        type: "笔记本电脑",
        brandModel: "ThinkPad X1",
        owner: "花满堂",
        user: "张三",
        department: "技术部",
        location: "北京机房",
        status: "active"
      },
      status: "counted",                // 'pending'(未盘到) | 'counted'(已盘) | 'exception'(异常)
      countedBy: "admin",              // 盘点人
      countedAt: "2026-09-08T15:42:11",// 盘点时间
      exceptionNote: "屏幕有划痕"      // status=exception 时必填
    }
  ]
}
```

---

## 四、服务端改动

### 文件：`server/src/routes/compat.js`

#### 4.1 扩展 KV 白名单（第 32-34 行）

**当前代码：**
```js
const KV_KEYS = ['userStateData', 'systemSettings', 'backupHistory',
    'assetCardTemplate', 'analyzedExcelFormats'];
const KV_PREFIXES = ['asset_userStateData_'];
```

**改为：**
```js
const KV_KEYS = ['userStateData', 'systemSettings', 'backupHistory',
    'assetCardTemplate', 'analyzedExcelFormats', 'inventory_sessions'];
const KV_PREFIXES = ['asset_userStateData_', 'inventory_session_'];
```

#### 4.2 viewer 角色写入放行（第 155-156 行）

**当前代码：**
```js
router.post('/save', async (ctx) => {
    requireWrite(ctx);
    const key = String(ctx.query.key || (ctx.request.body && ctx.request.body.key) || '').trim();
```

**改为：**
```js
router.post('/save', async (ctx) => {
    const key = String(ctx.query.key || (ctx.request.body && ctx.request.body.key) || '').trim();
    // 盘点键: 所有已登录用户(含 viewer)均可写
    const isInventoryKey = key === 'inventory_sessions' || key.startsWith('inventory_session_');
    if (!isInventoryKey) {
        requireWrite(ctx);
    }
```

#### 4.3 审计日志（可选，在 save 成功后补）

在 `/api/save` 成功写入后追加：
```js
if (isInventoryKey) {
    db.prepare('INSERT INTO audit_log (username, action, target, detail) VALUES (?, ?, ?, ?)')
        .run(ctx.state.user.username, 'inventory.save', key,
             JSON.stringify(ctx.request.body.value).substring(0, 200));
}
```

> **注意**：`isAllowedKvKey(key)` 函数（第 40-42 行）会因白名单扩展自动放行 `inventory_sessions` 和 `inventory_session_*`，无需额外修改。

---

## 五、前端常量定义

### 文件：`js/config.js`（第 43-57 行 STORAGE_KEYS 对象）

**追加 2 个常量：**
```js
const STORAGE_KEYS = {
    // ... 现有键不变 ...
    CUSTOM_OPTIONS_DEPARTMENT_DELETED: 'custom_options_department_deleted',
    // 盘点模块
    INVENTORY_SESSIONS: 'inventory_sessions',
    INVENTORY_SESSION_ITEM: 'inventory_session_'    // 前缀，实际 key 拼接批次 ID
}
```

---

## 六、前端模块设计

### 文件：`js/inventory.js`（新建）

#### 6.1 模块结构

```js
(function () {
    'use strict';

    // ====== 常量 ======
    const STORAGE_KEY_SESSIONS = 'inventory_sessions';
    const STORAGE_KEY_PREFIX = 'inventory_session_';

    // ====== 状态 ======
    let currentSession = null;       // 当前打开的批次明细
    let sessionsList = [];           // 批次列表缓存
    let currentMode = 'list';        // 'list' | 'detail' | 'scan' | 'report'
    let scanCallback = null;         // 扫码回调引用

    // ====== 持久化封装 ======
    async function loadSessions() {
        const data = await storageManager.getItem(STORAGE_KEY_SESSIONS);
        return Array.isArray(data) ? data : [];
    }
    async function saveSessions(list) {
        await storageManager.setItem(STORAGE_KEY_SESSIONS, list);
    }
    async function loadSessionDetail(id) {
        const data = await storageManager.getItem(STORAGE_KEY_PREFIX + id);
        return data || null;
    }
    async function saveSessionDetail(detail) {
        // 乐观锁: 对比 version
        const fresh = await loadSessionDetail(detail.id);
        if (fresh && fresh.version !== detail.version) {
            throw { code: 40901, message: '该盘点批次已被其他用户更新，请刷新后重试' };
        }
        detail.version = (detail.version || 0) + 1;
        detail.session.version = detail.version;
        detail.session.updatedAt = new Date().toISOString();
        // 同步列表项缓存字段
        await updateSessionsListEntry(detail.session);
        await storageManager.setItem(STORAGE_KEY_PREFIX + detail.id, detail);
        return detail;
    }
    async function updateSessionsListEntry(session) {
        const list = await loadSessions();
        const idx = list.findIndex(s => s.id === session.id);
        if (idx >= 0) {
            list[idx] = session;
        } else {
            list.unshift(session);
        }
        await saveSessions(list);
    }

    // ====== 业务操作 ======

    // 发起盘点
    async function createSession(name, scope) {
        const id = 'inv_' + new Date().toISOString().replace(/[-:T]/g, '').substring(0, 14);
        // 从 assetsData 按 scope 筛选
        let filtered = assetsData || [];
        if (scope.mode === 'department') {
            filtered = filtered.filter(a => a.owner === scope.department || a.department === scope.department);
        } else if (scope.mode === 'location') {
            filtered = filtered.filter(a => a.location === scope.location);
        }
        // 生成 items
        const items = filtered.map(a => ({
            assetId: a.id,
            snapshot: {
                type: a.type, brandModel: a.brandModel, owner: a.owner,
                user: a.user, department: a.department,
                location: a.location, status: a.status
            },
            status: 'pending',
            countedBy: null, countedAt: null, exceptionNote: null
        }));
        const now = new Date().toISOString();
        const session = {
            id, name, creator: (typeof ApiClient !== 'undefined' && ApiClient.user) ?
                (ApiClient.user.username || ApiClient.user.name || '用户') : '本地用户',
            creatorRole: (typeof ApiClient !== 'undefined' && ApiClient.user) ?
                (ApiClient.user.role || 'viewer') : 'local',
            createdAt: now, updatedAt: now,
            status: 'in_progress', scope,
            totalAssets: items.length, countedAssets: 0, exceptionCount: 0,
            version: 0
        };
        const detail = { id, session, items, version: 0 };
        await saveSessionDetail(detail);
        return detail;
    }

    // 标记单条
    async function markItem(assetId, status, exceptionNote) {
        if (!currentSession) return;
        const item = currentSession.items.find(i => i.assetId === assetId);
        if (!item) return;
        const oldStatus = item.status;
        item.status = status;
        item.exceptionNote = status === 'exception' ? (exceptionNote || '') : null;
        if (status !== 'pending') {
            item.countedBy = (typeof ApiClient !== 'undefined' && ApiClient.user) ?
                (ApiClient.user.username || ApiClient.user.name || '用户') : '本地用户';
            item.countedAt = new Date().toISOString();
        } else {
            item.countedBy = null;
            item.countedAt = null;
            item.exceptionNote = null;
        }
        // 更新缓存统计
        updateCachedCounts();
        // 持久化（乐观锁）
        try {
            await saveSessionDetail(currentSession);
        } catch (e) {
            // 冲突 → 重载
            if (e.code === 40901) {
                alert(e.message);
                currentSession = await loadSessionDetail(currentSession.id);
                renderSessionDetail(currentSession);
                return;
            }
            throw e;
        }
    }

    function updateCachedCounts() {
        if (!currentSession) return;
        const items = currentSession.items || [];
        currentSession.session.countedAssets = items.filter(i => i.status === 'counted').length;
        currentSession.session.exceptionCount = items.filter(i => i.status === 'exception').length;
    }

    // 完成盘点
    async function closeSession(id) {
        if (!currentSession) return;
        const pending = currentSession.items.filter(i => i.status === 'pending').length;
        if (pending > 0) {
            if (!confirm(`仍有 ${pending} 条资产未盘到，确定完成盘点？`)) return;
        }
        currentSession.session.status = 'completed';
        updateCachedCounts();
        await saveSessionDetail(currentSession);
    }

    // 删除批次
    async function deleteSession(id) {
        if (!confirm('确定删除此盘点批次？此操作不可撤销。')) return;
        const list = await loadSessions();
        const filtered = list.filter(s => s.id !== id);
        await saveSessions(filtered);
        // 删除明细键（storageManager 可能没有 removeItem，用 setItem 设为 null）
        try { await storageManager.setItem(STORAGE_KEY_PREFIX + id, null); } catch (e) {}
    }

    // ====== 扫码盘点 ======

    async function startScanMode() {
        if (!currentSession) return;
        // 检查环境
        if (typeof MobileBridge !== 'undefined' && MobileBridge.isCapacitor && MobileBridge.isCapacitor()) {
            // 移动端：原生扫码
            document.getElementById('inv-scan-overlay').classList.add('active');
            updateScanStatus();
            MobileBridge.scanForInventory(onScannedAsset);
        } else {
            // 桌面端降级：手动输入
            const assetId = prompt('请输入资产编号（桌面端不支持扫码，请手动输入）：');
            if (assetId) onScannedAsset(assetId.trim());
        }
    }

    function onScannedAsset(assetId) {
        if (!currentSession) return;
        const item = currentSession.items.find(i => i.assetId === assetId);
        if (!item) {
            showToast('资产 ' + assetId + ' 不在本批次范围内');
            return;
        }
        if (item.status === 'counted') {
            showToast('资产 ' + assetId + ' 已盘过');
            return;
        }
        markItem(assetId, 'counted', '').then(() => {
            updateScanStatus();
            // 刷新表格
            renderSessionDetail(currentSession);
        });
    }

    function updateScanStatus() {
        const counted = currentSession.items.filter(i => i.status === 'counted').length;
        const total = currentSession.items.length;
        const el = document.getElementById('inv-scan-status');
        if (el) el.textContent = `已扫到 ${counted} / ${total} 条`;
    }

    function stopScan() {
        if (typeof MobileBridge !== 'undefined' && MobileBridge.stopInventoryScan) {
            MobileBridge.stopInventoryScan();
        }
        document.getElementById('inv-scan-overlay').classList.remove('active');
    }

    // ====== 视图渲染 ======

    async function renderSessionsList() {
        const container = document.getElementById('inv-sessions-container');
        if (!container) return;
        sessionsList = await loadSessions();
        if (sessionsList.length === 0) {
            container.innerHTML = '<div style="text-align:center;padding:40px;color:var(--text-secondary);">' +
                '<i class="fas fa-clipboard" style="font-size:48px;margin-bottom:12px;display:block;"></i>' +
                '暂无盘点记录，点击上方"发起盘点"开始</div>';
            return;
        }
        container.innerHTML = sessionsList.map(s => {
            const pct = s.totalAssets > 0 ? Math.round(s.countedAssets / s.totalAssets * 100) : 0;
            return '<div class="inv-session-card" onclick="openSession(\'' + s.id + '\')">' +
                '<div class="inv-session-info">' +
                '<div class="inv-session-name">' + escapeHtml(s.name) +
                    '<span class="inv-status-badge inv-status-' + s.status + '">' +
                    (s.status === 'in_progress' ? '进行中' : '已完成') + '</span></div>' +
                '<div class="inv-session-meta">创建人：' + escapeHtml(s.creator) +
                    ' · ' + s.createdAt.substring(0, 16).replace('T', ' ') +
                    ' · 已盘 ' + s.countedAssets + '/' + s.totalAssets +
                    ' · 异常 ' + (s.exceptionCount || 0) + ' · ' + pct + '%</div>' +
                '</div>' +
                '<div style="display:flex;align-items:center;gap:8px;">' +
                '<span style="color:var(--text-secondary);font-size:20px;font-weight:600;">' + pct + '%</span>' +
                (s.status === 'in_progress' ?
                    '<button class="btn btn-sm btn-primary" onclick="event.stopPropagation();openSession(\'' + s.id + '\')">继续</button>'
                    : '<button class="btn btn-sm btn-secondary" onclick="event.stopPropagation();openSession(\'' + s.id + '\')">查看</button>') +
                '<button class="btn btn-sm btn-danger" onclick="event.stopPropagation();deleteSession(\'' + s.id + '\')">删除</button>' +
                '</div></div>';
        }).join('');
    }

    function renderSessionDetail(detail) {
        // 切换视图
        document.getElementById('inv-list-card').style.display = 'none';
        document.getElementById('inv-detail-container').style.display = '';
        // 统计
        const items = detail.items || [];
        const counted = items.filter(i => i.status === 'counted').length;
        const pending = items.filter(i => i.status === 'pending').length;
        const exception = items.filter(i => i.status === 'exception').length;
        const total = items.length;
        const pct = total > 0 ? Math.round(counted / total * 100) : 0;
        document.getElementById('inv-total').textContent = total;
        document.getElementById('inv-counted').textContent = counted;
        document.getElementById('inv-pending').textContent = pending;
        document.getElementById('inv-exception').textContent = exception;
        document.getElementById('inv-progress-bar').style.width = pct + '%';
        document.getElementById('inv-progress-text').textContent = pct + '%';
        // 标题
        document.querySelector('#inventory-page .page-title').innerHTML =
            '<i class="fas fa-clipboard-check"></i> ' + escapeHtml(detail.session.name);
        // 表格
        const filter = document.getElementById('inv-filter').value;
        const search = (document.getElementById('inv-search').value || '').toLowerCase();
        let filtered = items;
        if (filter !== 'all') filtered = filtered.filter(i => i.status === filter);
        if (search) filtered = filtered.filter(i =>
            (i.assetId || '').toLowerCase().includes(search) ||
            (i.snapshot && i.snapshot.brandModel || '').toLowerCase().includes(search)
        );
        const tbody = document.getElementById('inv-items-tbody');
        tbody.innerHTML = filtered.map(i => {
            const s = i.snapshot || {};
            const statusBadge = i.status === 'counted' ?
                '<span class="inv-item-status inv-status-counted"><i class="fas fa-check"></i> 已盘</span>' :
                i.status === 'exception' ?
                '<span class="inv-item-status inv-status-exception"><i class="fas fa-exclamation-triangle"></i> 异常</span>' :
                '<span class="inv-item-status inv-status-pending"><i class="fas fa-clock"></i> 未盘到</span>';
            const rowClass = i.status === 'counted' ? 'inv-row-counted' :
                i.status === 'exception' ? 'inv-row-exception' : '';
            return '<tr class="' + rowClass + '">' +
                '<td>' + escapeHtml(i.assetId) + '</td>' +
                '<td>' + escapeHtml(s.type || '-') + '</td>' +
                '<td>' + escapeHtml(s.brandModel || '-') + '</td>' +
                '<td>' + escapeHtml(s.user || '-') + '</td>' +
                '<td>' + escapeHtml(s.department || '-') + '</td>' +
                '<td>' + escapeHtml(s.location || '-') + '</td>' +
                '<td>' + statusBadge + '</td>' +
                '<td>' + escapeHtml(i.exceptionNote || '-') + '</td>' +
                '<td>' + escapeHtml(i.countedBy || '-') + '</td>' +
                '<td>' + (i.countedAt ? i.countedAt.substring(0, 16).replace('T', ' ') : '-') + '</td>' +
                '<td style="white-space:nowrap;">' +
                    '<button class="btn btn-sm btn-success" onclick="markItemUI(\'' + i.assetId + '\',\'counted\')">已盘</button> ' +
                    '<button class="btn btn-sm btn-warning" onclick="markItemUI(\'' + i.assetId + '\',\'exception\')">异常</button> ' +
                    '<button class="btn btn-sm btn-secondary" onclick="markItemUI(\'' + i.assetId + '\',\'pending\')">撤销</button>' +
                '</td></tr>';
        }).join('');
        // 完成按钮状态
        document.getElementById('inv-close-btn').style.display =
            detail.session.status === 'in_progress' ? '' : 'none';
        document.getElementById('inv-scan-btn').style.display =
            detail.session.status === 'in_progress' ? '' : 'none';
    }

    function backToList() {
        document.getElementById('inv-list-card').style.display = '';
        document.getElementById('inv-detail-container').style.display = 'none';
        document.querySelector('#inventory-page .page-title').innerHTML =
            '<i class="fas fa-clipboard-check"></i> 资产盘点';
        renderSessionsList();
    }

    // ====== 报告导出 ======

    function exportReport() {
        if (!currentSession) return;
        const items = currentSession.items || [];
        // 复用 libs/xlsx.full.min.js
        if (typeof XLSX === 'undefined') {
            // 动态加载
            const script = document.createElement('script');
            script.src = 'libs/xlsx.full.min.js';
            script.onload = () => doExport(items);
            document.head.appendChild(script);
        } else {
            doExport(items);
        }
    }

    function doExport(items) {
        const data = items.map(i => {
            const s = i.snapshot || {};
            return {
                '资产编号': i.assetId,
                '设备类型': s.type || '',
                '品牌型号': s.brandModel || '',
                '使用人': s.user || '',
                '部门': s.department || '',
                '地点': s.location || '',
                '盘点状态': i.status === 'counted' ? '已盘' : i.status === 'exception' ? '异常' : '未盘到',
                '异常备注': i.exceptionNote || '',
                '盘点人': i.countedBy || '',
                '盘点时间': i.countedAt ? i.countedAt.substring(0, 19).replace('T', ' ') : ''
            };
        });
        const ws = XLSX.utils.json_to_sheet(data);
        const wb = XLSX.utils.book_new();
        XLSX.utils.book_append_sheet(wb, ws, '盘点明细');
        XLSX.writeFile(wb, '盘点报告_' + currentSession.session.name + '_' +
            new Date().toISOString().substring(0, 10) + '.xlsx');
    }

    // ====== 工具函数 ======

    function escapeHtml(s) {
        if (!s) return '';
        return String(s).replace(/[&<>"']/g, c =>
            ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
    }

    function showToast(msg) {
        if (typeof showNotification === 'function') {
            showNotification(msg, 'info');
        } else {
            alert(msg);
        }
    }

    // ====== 异常备注弹窗 ======

    let pendingExceptionAssetId = null;
    function showExceptionModal(assetId) {
        pendingExceptionAssetId = assetId;
        const item = currentSession.items.find(i => i.assetId === assetId);
        const s = item ? (item.snapshot || {}) : {};
        document.getElementById('inv-exception-target').innerHTML =
            '<strong>资产编号：</strong>' + escapeHtml(assetId) + '<br>' +
            '<strong>品牌型号：</strong>' + escapeHtml(s.brandModel || '-');
        document.getElementById('inv-exception-note').value = item ? (item.exceptionNote || '') : '';
        document.getElementById('inv-exception-modal').style.display = '';
    }

    async function submitException() {
        const note = document.getElementById('inv-exception-note').value.trim();
        if (!note) { alert('请输入异常情况描述'); return; }
        await markItem(pendingExceptionAssetId, 'exception', note);
        document.getElementById('inv-exception-modal').style.display = 'none';
        renderSessionDetail(currentSession);
    }

    // ====== 发起盘点弹窗 ======

    function showNewModal() {
        document.getElementById('inv-new-name').value =
            new Date().getFullYear() + '年' + (new Date().getMonth() + 1) + '月盘点';
        document.getElementById('inv-new-scope-mode').value = 'all';
        document.getElementById('inv-new-scope-value-box').style.display = 'none';
        document.getElementById('inv-new-modal').style.display = '';
    }

    async function submitNew() {
        const name = document.getElementById('inv-new-name').value.trim();
        if (!name) { alert('请输入批次名称'); return; }
        const scope = { mode: document.getElementById('inv-new-scope-mode').value };
        if (scope.mode === 'department') {
            scope.department = document.getElementById('inv-new-scope-value').value;
        } else if (scope.mode === 'location') {
            scope.location = document.getElementById('inv-new-scope-value').value;
        }
        document.getElementById('inv-new-modal').style.display = 'none';
        currentSession = await createSession(name, scope);
        renderSessionDetail(currentSession);
    }

    function onScopeModeChange() {
        const mode = document.getElementById('inv-new-scope-mode').value;
        const box = document.getElementById('inv-new-scope-value-box');
        const label = document.getElementById('inv-new-scope-value-label');
        const select = document.getElementById('inv-new-scope-value');
        if (mode === 'all') {
            box.style.display = 'none';
            return;
        }
        box.style.display = '';
        select.innerHTML = '';
        if (mode === 'department') {
            label.textContent = '选择部门';
            // 从 custom_options 或 assetsData 去重
            const depts = [...new Set((assetsData || []).map(a => a.department).filter(Boolean))].sort();
            depts.forEach(d => select.innerHTML += `<option value="${escapeHtml(d)}">${escapeHtml(d)}</option>`);
        } else if (mode === 'location') {
            label.textContent = '选择地点';
            const locs = [...new Set((assetsData || []).map(a => a.location).filter(Boolean))].sort();
            locs.forEach(l => select.innerHTML += `<option value="${escapeHtml(l)}">${escapeHtml(l)}</option>`);
        }
    }

    // ====== 导航回调 ======

    window.renderInventoryPage = function () {
        backToList();
    };

    // 暴露给 onclick 调用的函数
    window.openSession = async function (id) {
        currentSession = await loadSessionDetail(id);
        if (currentSession) renderSessionDetail(currentSession);
    };
    window.deleteSession = deleteSession;
    window.markItemUI = async function (assetId, status) {
        if (status === 'exception') {
            showExceptionModal(assetId);
        } else {
            await markItem(assetId, status);
            renderSessionDetail(currentSession);
        }
    };

    // ====== DOM 事件绑定 ======

    document.addEventListener('DOMContentLoaded', function () {
        // 发起盘点
        document.getElementById('inv-new-btn').addEventListener('click', showNewModal);
        document.getElementById('inv-new-submit').addEventListener('click', submitNew);
        document.getElementById('inv-new-cancel').addEventListener('click', () =>
            document.getElementById('inv-new-modal').style.display = 'none');
        document.getElementById('inv-new-close').addEventListener('click', () =>
            document.getElementById('inv-new-modal').style.display = 'none');
        document.getElementById('inv-new-scope-mode').addEventListener('change', onScopeModeChange);

        // 刷新
        document.getElementById('inv-refresh-btn').addEventListener('click', renderSessionsList);

        // 详情页操作
        document.getElementById('inv-back-btn').addEventListener('click', backToList);
        document.getElementById('inv-filter').addEventListener('change', () =>
            currentSession && renderSessionDetail(currentSession));
        document.getElementById('inv-search').addEventListener('input', () =>
            currentSession && renderSessionDetail(currentSession));
        document.getElementById('inv-scan-btn').addEventListener('click', startScanMode);
        document.getElementById('inv-scan-stop').addEventListener('click', stopScan);
        document.getElementById('inv-export-btn').addEventListener('click', exportReport);
        document.getElementById('inv-close-btn').addEventListener('click', () => closeSession(currentSession.id));

        // 异常弹窗
        document.getElementById('inv-exception-close').addEventListener('click', () =>
            document.getElementById('inv-exception-modal').style.display = 'none');
        document.getElementById('inv-exception-cancel').addEventListener('click', () =>
            document.getElementById('inv-exception-modal').style.display = 'none');
        document.getElementById('inv-exception-submit').addEventListener('click', submitException);
    });
})();
```

---

## 七、index.html 改动

### 7.1 侧边栏菜单项（第 459 行后插入）

```html
<div class="menu-item" data-target="inventory">
    <i class="fas fa-clipboard-check"></i>
    <span>资产盘点</span>
</div>
```

### 7.2 主内容区页面（在 users-page 之前插入）

```html
<div class="page-content" id="inventory-page">
    <div class="header">
        <h1 class="page-title"><i class="fas fa-clipboard-check"></i> 资产盘点</h1>
        <div class="header-actions">
            <button class="btn btn-primary" id="inv-new-btn"><i class="fas fa-plus"></i> 发起盘点</button>
            <button class="btn btn-secondary" id="inv-refresh-btn"><i class="fas fa-sync-alt"></i> 刷新</button>
        </div>
    </div>

    <!-- 批次列表 -->
    <div class="card" id="inv-list-card">
        <div class="card-header"><h2 class="card-title"><i class="fas fa-list"></i> 盘点批次</h2></div>
        <div id="inv-sessions-container"></div>
    </div>

    <!-- 批次详情 -->
    <div id="inv-detail-container" style="display:none;">
        <div class="stats-container">
            <div class="stat-card"><div class="stat-value" id="inv-total">0</div><div class="stat-label">应盘</div></div>
            <div class="stat-card"><div class="stat-value" id="inv-counted"">0</div><div class="stat-label">已盘</div></div>
            <div class="stat-card"><div class="stat-value" id="inv-pending">0</div><div class="stat-label">未盘到</div></div>
            <div class="stat-card"><div class="stat-value" id="inv-exception">0</div><div class="stat-label">异常</div></div>
        </div>
        <div class="card">
            <div style="display:flex;align-items:center;gap:12px;">
                <div style="flex:1;height:10px;background:var(--bg-soft);border-radius:5px;overflow:hidden;">
                    <div id="inv-progress-bar" style="height:100%;background:var(--success-color);transition:width .3s;width:0%;"></div>
                </div>
                <span id="inv-progress-text" style="color:var(--text-secondary);font-size:13px;">0%</span>
            </div>
        </div>
        <div class="card">
            <div style="display:flex;flex-wrap:wrap;gap:10px;align-items:center;">
                <select id="inv-filter" class="form-input" style="width:auto;">
                    <option value="all">全部</option>
                    <option value="counted">已盘</option>
                    <option value="pending">未盘到</option>
                    <option value="exception">异常</option>
                </select>
                <input type="text" id="inv-search" class="form-input" placeholder="搜索编号/型号" style="flex:1;min-width:200px;">
                <button class="btn btn-success" id="inv-scan-btn"><i class="fas fa-qrcode"></i> 扫码盘点</button>
                <button class="btn btn-secondary" id="inv-export-btn"><i class="fas fa-file-export"></i> 导出报告</button>
                <button class="btn btn-primary" id="inv-close-btn"><i class="fas fa-check-double"></i> 完成盘点</button>
                <button class="btn btn-secondary" id="inv-back-btn"><i class="fas fa-arrow-left"></i> 返回列表</button>
            </div>
        </div>
        <div class="card">
            <table style="width:100%;border-collapse:collapse;font-size:13px;">
                <thead><tr style="background:var(--bg-soft);">
                    <th style="padding:10px 8px;text-align:left;">资产编号</th>
                    <th>设备类型</th><th>品牌型号</th><th>使用人</th>
                    <th>部门</th><th>地点</th><th>盘点状态</th>
                    <th>异常备注</th><th>盘点人</th><th>盘点时间</th><th>操作</th>
                </tr></thead>
                <tbody id="inv-items-tbody"></tbody>
            </table>
        </div>
    </div>

    <!-- 发起盘点弹窗 -->
    <div id="inv-new-modal" class="modal" style="display:none;">
        <div class="modal-content" style="max-width:520px;margin-top:12vh;">
            <span class="close-btn" id="inv-new-close">&times;</span>
            <h2 class="modal-title"><i class="fas fa-plus-circle"></i> 发起盘点</h2>
            <div style="padding:20px 0;">
                <div style="margin-bottom:16px;">
                    <label style="display:block;margin-bottom:6px;font-weight:600;">批次名称</label>
                    <input type="text" id="inv-new-name" class="form-input" placeholder="如：2026年9月例行盘点">
                </div>
                <div style="margin-bottom:16px;">
                    <label style="display:block;margin-bottom:6px;font-weight:600;">盘点范围</label>
                    <select id="inv-new-scope-mode" class="form-input">
                        <option value="all">全部资产</option>
                        <option value="department">按部门</option>
                        <option value="location">按地点</option>
                    </select>
                </div>
                <div id="inv-new-scope-value-box" style="display:none;margin-bottom:16px;">
                    <label id="inv-new-scope-value-label" style="display:block;margin-bottom:6px;font-weight:600;">选择</label>
                    <select id="inv-new-scope-value" class="form-input"></select>
                </div>
                <div style="display:flex;gap:10px;justify-content:flex-end;">
                    <button class="btn btn-secondary" id="inv-new-cancel">取消</button>
                    <button class="btn btn-primary" id="inv-new-submit">创建</button>
                </div>
            </div>
        </div>
    </div>

    <!-- 异常备注弹窗 -->
    <div id="inv-exception-modal" class="modal" style="display:none;">
        <div class="modal-content" style="max-width:480px;margin-top:15vh;">
            <span class="close-btn" id="inv-exception-close">&times;</span>
            <h2 class="modal-title"><i class="fas fa-exclamation-triangle"></i> 标记异常</h2>
            <div style="padding:20px 0;">
                <div id="inv-exception-target" style="margin-bottom:12px;padding:12px;background:var(--bg-soft);border-radius:8px;"></div>
                <textarea id="inv-exception-note" rows="4" class="form-input" placeholder="请输入异常情况描述..."></textarea>
                <div style="display:flex;gap:10px;justify-content:flex-end;margin-top:12px;">
                    <button class="btn btn-secondary" id="inv-exception-cancel">取消</button>
                    <button class="btn btn-warning" id="inv-exception-submit">确认异常</button>
                </div>
            </div>
        </div>
    </div>

    <!-- 扫码遮罩（移动端） -->
    <div id="inv-scan-overlay" style="display:none;position:fixed;inset:0;background:rgba(0,0,0,.85);z-index:9999;align-items:center;justify-content:center;flex-direction:column;color:#fff;">
        <i class="fas fa-qrcode" style="font-size:64px;margin-bottom:20px;"></i>
        <h2 style="margin-bottom:8px;">扫码盘点中</h2>
        <p id="inv-scan-status" style="margin-bottom:24px;color:#ccc;">已扫到 0 条</p>
        <button class="btn btn-danger" id="inv-scan-stop"><i class="fas fa-stop"></i> 结束扫码</button>
    </div>
</div>
```

### 7.3 底部脚本引用（在 init.js 之前插入）

```html
<script src="js/inventory.js?v=1.0.0" defer></script>
```

---

## 八、navigation.js 注册回调

### 文件：`js/navigation.js` 第 93 行 `pageHandlers` 对象内追加

```js
const pageHandlers = {
    // ... 现有不变 ...
    'inventory': function() {
        if (typeof renderInventoryPage === 'function') {
            renderInventoryPage();
        }
    },
    // ...
};
```

---

## 九、styles.css 追加样式

### 文件：`styles.css`（末尾追加，全部用 CSS 变量保证主题适配）

```css
/* ==================== 盘点模块 ==================== */

.inv-session-card {
    display: flex; align-items: center; justify-content: space-between;
    padding: 16px 20px; margin-bottom: 12px;
    background: var(--bg-surface); border: 1px solid var(--gray-border);
    border-radius: 8px; transition: var(--transition-base); cursor: pointer;
}
.inv-session-card:hover {
    box-shadow: var(--card-shadow-hover); border-color: var(--primary-color);
}
.inv-session-info { flex: 1; min-width: 0; }
.inv-session-name { font-size: 15px; font-weight: 600; color: var(--text-color); margin-bottom: 4px; }
.inv-session-meta { font-size: 12px; color: var(--text-secondary); }

.inv-status-badge {
    display: inline-block; padding: 2px 10px; border-radius: 10px;
    font-size: 12px; margin-left: 8px; font-weight: 500;
}
.inv-status-in_progress { background: var(--bg-tag); color: var(--brand); }
.inv-status-completed { background: rgba(82,196,26,.12); color: var(--success-color); }

.inv-item-status {
    display: inline-flex; align-items: center; gap: 4px;
    padding: 2px 10px; border-radius: 10px; font-size: 12px; font-weight: 500;
}
.inv-status-counted { background: rgba(82,196,26,.12); color: var(--success-color); }
.inv-status-pending { background: var(--bg-soft); color: var(--text-secondary); }
.inv-status-exception { background: rgba(245,34,45,.12); color: var(--danger-color); }

.inv-row-counted { background: rgba(82,196,26,.04); }
.inv-row-exception { background: rgba(245,34,45,.04); }

#inv-scan-overlay { display: none; }
#inv-scan-overlay.active { display: flex; }
```

---

## 十、mobile-bridge.js 扩展

### 文件：`js/mobile-bridge.js`

在现有 `window.MobileBridge` 对象追加 2 个方法：

```js
// 循环扫码盘点（不跳详情，扫到后回调，自动继续下一轮）
async function scanForInventory(onScanCallback) {
    if (!BarcodeScanner) {
        alert('扫码插件未加载');
        return;
    }
    try {
        var status = await BarcodeScanner.checkPermission({ force: true });
        if (!status.granted) { alert('需要相机权限'); return; }
    } catch (e) { /* 忽略 */ }

    window._invScanActive = true;
    while (window._invScanActive) {
        var result = null;
        try {
            document.body.style.opacity = '0';
            result = await BarcodeScanner.startScan();
            document.body.style.opacity = '1';
        } catch (e) {
            document.body.style.opacity = '1';
            break;
        }
        if (!result || !result.content) continue;

        var match = result.content.match(/编码[：:]\s*(.+)/);
        var assetId = match ? match[1].trim() : null;
        if (assetId && typeof onScanCallback === 'function') {
            onScanCallback(assetId, result.content);
        }
    }
}

function stopInventoryScan() {
    window._invScanActive = false;
}

// 追加到 window.MobileBridge
window.MobileBridge.scanForInventory = scanForInventory;
window.MobileBridge.stopInventoryScan = stopInventoryScan;
```

---

## 十一、实施顺序

| 步骤 | 文件 | 改动 | 依赖 |
|---|---|---|---|
| 1 | `server/src/routes/compat.js` | KV 白名单 + viewer 放行 + 审计 | 无 |
| 2 | `js/config.js` | STORAGE_KEYS 加 2 个常量 | 无 |
| 3 | `js/inventory.js` | 新建完整模块 | 步骤 2 |
| 4 | `index.html` | 菜单项 + 页面 div + script 引用 | 步骤 3 |
| 5 | `js/navigation.js` | pageHandlers 注册 | 步骤 3 |
| 6 | `styles.css` | 追加样式 | 无 |
| 7 | `js/mobile-bridge.js` | 追加扫码方法 | 无 |
| 8 | 热上传到 192.168.40.247 | pscp + plink | 步骤 1-7 |

---

## 十二、验证方案

### 桌面端验证
1. 登录系统 → 侧边栏出现「资产盘点」菜单
2. 点击进入 → 空状态提示正常
3. 点「发起盘点」→ 输入名称 + 选范围 → 创建成功
4. 进入明细页 → 表格列出范围内所有资产（全部 pending 灰色）
5. 统计卡显示：应盘=N, 已盘=0, 未盘到=N, 异常=0
6. 点「已盘」→ 行变绿 → 统计更新
7. 点「异常」→ 弹备注框 → 输入 → 确认 → 行变红 + 备注显示
8. 点「撤销」→ 回到灰色
9. 筛选器切换正常（全部/已盘/未盘到/异常）
10. 搜索框输入编号/型号 → 表格过滤
11. 点「完成盘点」→ 确认 → 状态变 completed
12. 返回列表 → 批次可见 + 进度 100%
13. 再次进入 → 只读模式（无扫码/完成按钮）
14. 点「导出报告」→ 下载 xlsx 文件
15. 删除批次 → 列表更新

### 移动端验证（Capacitor APK）
16. 点「扫码盘点」→ 全屏遮罩 → 原生相机
17. 扫标签二维码 → 自动标记已盘 → 计数+1
18. 重复扫 → 已盘提示
19. 点「结束扫码」→ 回到明细页
20. 深色主题下所有元素可读

---

## 十三、关键代码参考位置

| 需要参考的现有代码 | 位置 | 参考目的 |
|---|---|---|
| kv_store 白名单 | `server/src/routes/compat.js:32-42` | 扩展 KV_KEYS / KV_PREFIXES |
| /api/save 写入逻辑 | `server/src/routes/compat.js:155-198` | viewer 放行 + 审计 |
| requireWrite 权限检查 | `server/src/auth.js:62-64` | viewer 角色限制 |
| 侧边栏菜单模式 | `index.html:430-462` | 新菜单项格式 |
| 页面切换逻辑 | `js/navigation.js:21-64` | switchPage + pageHandlers |
| assetsData 字段 | `js/assets.js:74-97` | 快照字段名 |
| storageManager | `js/storage.js:988-1014` | 三模式持久化 |
| 统计卡样式 | `js/dashboard.js` + `styles.css` | stat-card 复用 |
| XLSX 导出 | `js/import-export.js` | libs/xlsx.full.min.js 用法 |
| 移动端扫码 | `js/mobile-bridge.js:31-84` | BarcodeScanner 用法 |
| CSS 变量 | `styles.css:105-167` | 主题色变量名 |

---

## 十四、热上传命令

```powershell
# 修改完前端文件后，热上传到服务器（无需重启服务）
$pscp = "D:\Program Files\PuTTY\pscp.exe"
$plink = "D:\Program Files\PuTTY\plink.exe"
$pw = "<SSH密码>"
Set-Location "d:\Users\Administrator\Desktop\固定资产管理系统exe离线便携版v1.1"

# 上传改动文件
& $pscp -batch -pw $pw "js\inventory.js" "hmt@192.168.40.247:/tmp/"
& $pscp -batch -pw $pw "js\mobile-bridge.js" "hmt@192.168.40.247:/tmp/"
& $pscp -batch -pw $pw "js\config.js" "hmt@192.168.40.247:/tmp/"
& $pscp -batch -pw $pw "js\navigation.js" "hmt@192.168.40.247:/tmp/"
& $pscp -batch -pw $pw "index.html" "hmt@192.168.40.247:/tmp/"
& $pscp -batch -pw $pw "styles.css" "hmt@192.168.40.247:/tmp/"

# 服务端文件需要 sudo 复制
& $pscp -batch -pw $pw "server\src\routes\compat.js" "hmt@192.168.40.247:/tmp/"

# 部署
& $plink -ssh -batch -pw $pw hmt@192.168.40.247 "echo <sudo密码> | sudo -S cp /tmp/compat.js /opt/asset-server/server/src/routes/compat.js && echo <sudo密码> | sudo -S cp /tmp/inventory.js /opt/asset-server/js/inventory.js && echo <sudo密码> | sudo -S cp /tmp/mobile-bridge.js /opt/asset-server/js/mobile-bridge.js && echo <sudo密码> | sudo -S cp /tmp/config.js /opt/asset-server/js/config.js && echo <sudo密码> | sudo -S cp /tmp/navigation.js /opt/asset-server/js/navigation.js && echo <sudo密码> | sudo -S cp /tmp/index.html /opt/asset-server/index.html && echo <sudo密码> | sudo -S cp /tmp/styles.css /opt/asset-server/styles.css && echo <sudo密码> | sudo -S chown -R asset:asset /opt/asset-server/ && echo <sudo密码> | sudo -S systemctl restart asset-server && echo DEPLOY_DONE"
```

> **注意**：服务端文件（compat.js）改完后需要 `systemctl restart asset-server` 重启服务；前端文件不需要重启。
